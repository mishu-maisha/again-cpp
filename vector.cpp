#include <iostream>
#include <vector>
using namespace std;
int main (){
    vector<int>v1;
    v1={1,2,3};
    
    for(int val: v1){
        cout<<val<<" ";
    }
    cout<<endl;
  
 //push_back() for adding value at last index
 //pop _back() to delete the value at last index
 //vecname.size() to calculate size
 //vecname.capacity() to calculate capacity of
 //vecname.front() to find value at front   
 //vecname.back() to find value at back
 //vecname.at(index) value at particulur index    
    
}